// Creado con el mandato CLI
//  nest g mo common
// No es necesario por ahora, pero se crea por si en algún momento lo necesitáramos.
import { Module } from '@nestjs/common';

@Module({})
export class CommonModule {}
